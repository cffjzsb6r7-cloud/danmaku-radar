# 弹幕雷达 Danmaku Radar · AI 交接任务书（Handover）

> 使用方法：把本文件全文直接粘贴给 AI 编程助手（Codex / Claude / 其他编程 AI）。
> 如果工作区已有本仓库代码，基于「现有进度」继续完善；如果从零开始，就按本文件实现，下面的需求不变。
> 本文件只写「要做什么」和「做成什么样」，技术方案由 AI 决定；涉及真实数据与合规的部分已注明。

## 开场指令（给 AI 的第一段话）

你是全栈产品工程师。请根据本任务书实现 / 继续实现产品「弹幕雷达 Danmaku Radar」。开始前先输出实现计划：一句话技术选型 + 页面结构 + 数据模型 + 订阅 / 邮件推送方案；确认后再动手编码。每个功能完成即保持可用，并在 README 记录进度与取舍。

## 一、这是什么软件（产品一句话）

**B站热度 Agent + 网页（前后端分离）**：自动抓取**哔哩哔哩真实排行榜**，生成「内容热榜 Top 20（按一周点赞增量）」和「话题热榜 Top 20（按一周发布增量）」；用户**订阅邮箱后，每周一自动收到中英双语周报**；网页提供 Learn from me（拆解爆款为什么火）和 Clone this idea（把爆款变成自己的选题）。真实数据、真实数据库、稳定对外可用。

## 二、背景与目标用户

- 开发者：在校大学生（金融学专业，非程序员），备考并求职，本项目为求职作品（展示自动化、爬虫、后端 API、前端、产品能力）。
- 目标用户：不追热点但需要知道圈内动态的人（求职者、内容创作者、运营、学习者）。
- 核心价值：每周 5 分钟知道最近什么火；喜欢的能拆解、能克隆成自己的选题；订阅后不用访问，周报自动到邮箱。

## 三、现有进度（已完成，可运行）

当前仓库为**完整可用的真实全栈项目**：

- 后端（FastAPI + SQLite 真实数据库）：真实抓取 B站排行榜（含风控访客 cookie 引导、重试降级）；数据库表 posts / topics / subscribers / digests；API：/api/health、/api/trends、/api/subscribe、/api/unsubscribe、/api/digests、/api/refresh。
- 前端（独立静态站）：HEY 风格完整落地页（初衷痛点 / 内容热榜 / 话题热榜 / Learn / Clone / 订阅说服 / FAQ / 收藏），调用后端 API，后端不可用时自动回退静态缓存数据，带「真实数据 · B站」标识。
- 中英双语周报：自动生成中文 + English 两版，SMTP 邮件推送（未配置 SMTP 自动跳过）。
- 自动化：GitHub Actions 每周一自动 抓取 → 入库 → 发邮件 → 更新网站；Dockerfile + render.yaml 可公开部署后端。
- 自测：backend/selftest.py 全部通过。
- 文件：backend/（app/crawler.py、db.py、digest.py、emailer.py、main.py、refresh.py、send_digest.py、selftest.py）、website/（index.html、style.css、app.js）、data/（danmaku.db、latest.json、周报）、.github/workflows/weekly-digest.yml、Dockerfile、render.yaml、.env.example。

## 四、MVP 功能清单（必须全部可用）

### Part 1 · 自动抓取与双榜（真实数据）

1. **自动抓取哔哩哔哩热榜**：每周抓取 B站真实排行榜（标题、UP主、分区、播放 / 点赞 / 评论 / 收藏、直达链接、真实公开评论摘要）。B站有风控，需访客 cookie（buvid3）引导；请求重试；失败时保留旧数据并明确标注，不编造数据。
2. **内容热榜 Top 20**：按「一周点赞增量」排序；每条含排名、标题、作者、**直达原帖链接**、**原帖话题**、互动数据（总赞 / 本周增量 / 评论 / 收藏 / 播放）、**评论区内容总结**。
3. **话题热榜 Top 20**：按「一周发布增量」排序；每个话题含排名、话题、本周新增、总量、趋势（由真实分区与标题聚合）。

### Part 2 · Learn from me（拆解爆款）

4. 用户选择热榜内容或粘贴标题 / 内容 / 标签 → 规则化拆解（不依赖 LLM）：标题套路、关键词、标签、内容结构（首句钩子 / 句子数 / emoji / 段落）、互动信号（收藏比 / 讨论度）；输出「爆款拆解卡」，可收藏。

### Part 3 · Clone this idea（克隆创意）

5. 基于爆款模板化生成 3-5 个选题：标题公式、标签建议、要点提纲、差异化角度（换人群 / 场景 / 平台）；可收藏进「灵感清单」。

### Part 4 · 邮箱订阅推送（中英双语）

6. 首页与订阅区放邮箱输入框；订阅成功入库、重复邮箱提示；支持退订（邮件内退订链接 + API）。
7. 每周一自动：抓取 → 入库 → 生成**中英双语周报** → SMTP 发送给所有订阅者 → 更新网站；未配置 SMTP 时跳过发送但不影响更新。
8. 合规：订阅需用户同意；任何邮件含退订方式；不群发垃圾。

### 通用

9. 收藏 / 灵感清单（本地）。
10. 落地页：初衷与痛点、功能区块、订阅说服、FAQ，交互完整，设计简约好看。

## 五、页面与内容规范（单页落地站）

1. Hero：大标题 + 订阅输入框
2. 热词跑马灯 / 数据条（收录数 / 总点赞增量 / 话题数，数字滚动）
3. 初衷与痛点（3 张痛点卡 + 一段「为什么做」的话）
4. 内容热榜 Top 20（浏览器窗口卡片 + 分类筛选 chips + 直达链接 + 评论总结）
5. 话题热榜 Top 20（排名 + 增长条）
6. Learn from me（左文右表单 → 拆解卡）
7. Clone this idea（左文右表单 → 选题卡）
8. 订阅说服区（紫色渐变，3 个卖点）
9. FAQ（手风琴，6 条）
10. 收藏与页脚

## 六、设计规范

- 参考 HEY.com 风格：背景纸白 #F9F7F5、正文墨黑 #231C33、品牌紫 #5522FA、马卡龙区块（canary 黄 #FFF5CA / lavender 紫 #EAE8FE / mint 薄荷 #D8F9F0 / peach 蜜桃 #FEF2ED / sky 蓝 #EEF8FF）。
- 标题思源宋体（Noto Serif SC 900）、正文思源黑体（Noto Sans SC）；2px 黑色描边、圆角卡片、留白充足。
- 交互：滚动浮现、数字滚动、跑马灯、导航高亮、卡片悬停、筛选切换、FAQ 手风琴、订阅成功反馈。

## 七、数据与数据库

- 真实数据库 SQLite（表：posts / topics / subscribers / digests / meta），每周一份快照（先清后插），点赞增量按周对比真实计算。
- 对外数据文件 data/latest.json（内容热榜 + 话题热榜），供前端与部署使用。

## 八、长期愿景（MVP 完成后逐步做，不阻塞 MVP；较费 token / 成本的放后面）

1. AI 深度分析：解释「为什么火」、受众画像、趋势预测（需 LLM API，费 token）。
2. AI 一键成稿：从「克隆创意」直接生成笔记 / 视频脚本正文。
3. 自动发布：一键发布到平台（需平台授权，合规风险高）。
4. 多平台适配器：Instagram / X（Twitter）/ 微博 / 抖音（各自需评估登录态、付费 API 与合规）。
5. 博主追踪：UP主内容与涨粉曲线。
6. 个性化订阅：用户选择关注方向，只推送相关周报；多渠道（微信 / Telegram / 钉钉）。
7. 社区与资料库：公开热榜历史库、分享拆解卡与选题。
8. 商业化：品牌方内容分析、投放建议；高级主题。

## 九、验收标准（MVP 全部满足才算完成）

- B站真实数据链路可用：排行榜 → SQLite → API → 前端展示；抓取失败时明确提示且不中断服务。
- 内容热榜 Top 20 与话题热榜 Top 20 均按序展示，含直达链接、话题、评论总结。
- Learn / Clone 可用且结果可收藏。
- 邮箱订阅可用：订阅成功入库、重复提示、退订成功；每周任务能生成中英双语周报并发邮件（SMTP 配好后）。
- 前后端分离：前端独立部署（GitHub Pages），后端独立部署（Render / Docker），CORS 正常。
- 网页简约好看、交互完整、响应式；README 与功能一致；自测通过。

## 十、实现要求（约束，不是技术方案）

- **真实优先**：数据必须来自真实公开接口（B站），禁止编造数据；抓取失败降级为旧数据 + 明确提示。
- 免费起步：MVP 不接付费 LLM；邮件用 SMTP（用户自有邮箱授权码）；数据库用 SQLite（可提交快照）。
- 前后端分离：后端 API（FastAPI）+ 前端静态站；前端调用后端，后端不可用回退静态缓存。
- 稳定可靠：请求重试、异常兜底、数据库 WAL + 索引、每周快照；GitHub Actions 每周自动更新。
- 合规：低频抓取、尊重平台条款；评论接口受限时用真实简介替代并如实标注。
- 中文界面、HEY 风格、简约好看、响应式。

## 十一、给你的输出要求

直接开始实现 / 继续完善，不要只给方案。完成后的总结包含：实现了哪些功能、如何运行、如何配置订阅推送（SMTP / Secrets）、如何部署（GitHub Pages + Render）、自测结果、以及下一步建议。
